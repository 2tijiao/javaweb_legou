<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lilma.legou.mapper.SalerMapper">
    <!--动态sql-->
    <!--id="test"：这个查询操作的唯一标识符，可以在Java代码中通过这个id来调用这个查询。
resultType="com.lilma.pojo.Goods"：指定了查询结果应该映射到的Java对象类型，这里是Goods类。-->
    <select id="getGoods" resultType="com.lilma.legou.pojo.Goods">
        SELECT g.*
        FROM goods g
        WHERE g.categoryid = (
            SELECT s.categoryid
            FROM saler s
            WHERE s.id = #{id}
        )
    </select>
</mapper>